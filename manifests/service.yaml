apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: archon-knowledge-base
  labels:
    app: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8090
    targetPort: 8090
    protocol: TCP
    name: http
  selector:
    app: mcp-server
